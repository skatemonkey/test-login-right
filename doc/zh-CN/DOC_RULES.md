## 文档结构规则

所有文档都应遵循清晰的层级结构。

* 从**高层概览**开始。
* 避免出现过大的**平铺列表**。
* 每一层保持在 **3–7 个组件**之间。
* 如果项目很多，就按**类别分组**。
* 按照**自上而下**的方式逐层展开说明。

### 文件结构

* 不要在一个文件里放入过多主题。
* 必要时将大主题拆分为**独立文件**。
* 使用**文件中的文件结构**（主文件链接到子文档）。
* 保持每个文件只聚焦**一个明确主题**。

目标：文档应当**模块化、层级化、易于导航**。
